[project]
name = "macregistry"
version = "0.1.0"

description = "Simple HTTP API for gather MAC Addresses within LAN segment"
readme = "README.md"

requires-python = ">=3.13"

dependencies = [
    "fastapi~=0.54",
    "uvicorn~=0.11",
    "loguru~=0.7",
    "scapy~=2.6",
    "typer~=0.16.0",
]
